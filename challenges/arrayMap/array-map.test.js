const { mapOne } = require('./array-map.js');

test.skip('test', () => {
  it('console.logs', () => {
    // console.log(mapOne);
  });
});

test.skip('it will map through', () => {
  it('maps through an array', () => {
    expect([{}]).toEqual([{}]);
  });
});